spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
app:
  ai:
    mock-mode: true
    anthropic:
      api-key: ""
  jwt:
    secret: test-secret-key-for-junit-only-not-for-production-use
